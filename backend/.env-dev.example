# BrainDrive Development Environment Configuration
# Copy this to .env-dev and fill in your values

# Application
APP_ENV=dev  # IMPORTANT: defaults to "production" if not set. Set to "dev" for local development.
DEBUG=true   # Defaults to false. Set true for local development.

# Security
SECRET_KEY=your-secret-key-here-change-in-production

# Service Authentication Tokens
# Generate new tokens with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# WARNING: Keep these secret! These tokens authenticate internal services.
PLUGIN_RUNTIME_TOKEN=generate-a-secure-token-here
JOB_WORKER_TOKEN=generate-a-secure-token-here
PLUGIN_LIFECYCLE_TOKEN=generate-a-secure-token-here

# Rate Limiting
MAX_REQUEST_SIZE=5242880  # 5MB in bytes

# Database
DATABASE_URL=sqlite:///braindrive.db

# CORS (set to your frontend URL in production)
# Leave empty for development (wildcard allowed)
CORS_ORIGINS=

# Allowed Hosts (comma-separated or JSON array)
# In production, set to your actual domain(s).
# ALLOWED_HOSTS='["yourdomain.com", "api.yourdomain.com"]'

# Feature Gates
ENABLE_TEST_ROUTES=true       # Defaults to false. Enables test/debug API routes.
ALLOW_REGISTRATION=true       # Defaults to true. Set false to disable self-signup.
ENABLE_API_DOCS=true          # Defaults to true. Set false to hide Swagger/ReDoc/OpenAPI.

# Admin (optional, for future admin JWT separation)
# ADMIN_SECRET_KEY=generate-a-separate-secret-for-admin
